in Json body;
in Url url;

WriteLine("Hello from Morph!");
WriteLine(url.urlString);
WriteLine(url.path);
WriteLine(url.query);
WriteLine(url.localPath);

SetHeader("X-Custom-Header", "MyValue");
SetResponseCode(201);